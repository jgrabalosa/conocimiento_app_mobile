name: conocimiento_app_mobile
description: "Norday — App de Conocimiento"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  # El motor del ecosistema: red, temas, gamificación, mascota, onboarding y
  # preferencias. Lo que vive ahí no sabe de píldoras ni de categorías.
  norday_flutter_core:
    git:
      url: https://github.com/jgrabalosa/norday_flutter_core.git
      ref: a873d8f99fdf42b7e9d9434d8c5d7d545ea525bd
  intl: any
  cupertino_icons: ^1.0.8
  http: ^1.2.0
  shared_preferences: 2.2.2
  google_fonts: ^8.1.0
  lucide_icons_flutter: ^3.1.15
  # Todavía sin usar: lo necesita el detalle de píldora (Paso F4), que pinta
  # el contenido en Markdown. Se declara ya para no volver a tocar el pubspec.
  flutter_markdown_plus: ^1.0.10
  # Compartir la píldora desde el detalle. La 13 ya no usa `Share.share`:
  # es `SharePlus.instance.share(ShareParams(...))`, lo otro está deprecado.
  share_plus: ^13.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    # Las fuentes de las cuatro identidades, empaquetadas. `google_fonts` mira
    # primero en los assets y sólo baja por HTTP si no encuentra el fichero,
    # así que basta con listarlas aquí: NO van en la sección `fonts:`, y sus
    # nombres no se pueden tocar porque el paquete las busca por el nombre con
    # el que las sirve la API de Google.
    - google_fonts/
